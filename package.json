{
  "name": "sigil",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "v0 efficiency-frontier audit harness: corpus -> stubbed-gateway panel run -> calibrated judge (ECE/Brier/reliability) -> Pass^k reliability -> Pareto frontier + equal-quality savings. Fixture-driven; never calls a real model/key/network.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@types/node": "^22.10.2",
    "eslint": "^9.17.0",
    "fast-check": "^4.8.0",
    "typescript": "^5.7.2",
    "typescript-eslint": "^8.18.2",
    "vitest": "3.2.6"
  },
  "pnpm": {
    "overrides": {
      "vite": "6.4.3"
    }
  }
}
